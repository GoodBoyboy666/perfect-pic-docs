---
sidebar_position: 2
---

# 上传与配额

## 文件大小限制

通过 `系统设置`->`上传`->`单个文件最大大小`，限制用户上传的单个图片的大小。

## 文件类型

通过 `系统设置`->`上传`->`允许上传的文件扩展名`，限制用户上传的文件类型。

## 默认配额

通过 `系统设置`->`上传`->`默认用户存储配额`，设定用户默认的存储配额。如需自定义单个用户配额，可以在`用户管理`中进行单独设置。
